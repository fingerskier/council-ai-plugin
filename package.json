{
  "name": "council-claude-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "A dual-host (Claude Code + Codex) plugin that convenes a council of named seats plus a chair.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  }
}
